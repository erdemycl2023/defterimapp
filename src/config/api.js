const BASE_URL = 'http://192.168.1.101:8080/defterim/mobile';

const API_ENDPOINTS = {
  LOGIN: `${BASE_URL}/login`,
  LOGOUT: `${BASE_URL}/logout`,
  VALIDATE: `${BASE_URL}/validate`,
  FIRMA_LIST: `${BASE_URL}/firmaList`,
};

export default API_ENDPOINTS; 